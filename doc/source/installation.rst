============
Installation
============

At the command line::

    $ pip install inspector-hooks

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv inspector-hooks
    $ pip install inspector-hooks
